/**
 * Git utils
 */

/**
 * Parse .gitignore file, return a list of dirs and files that should be ignored
 */
function parseGitIgnoreFile(gitignoreFilePath) {

}

module.exports = {
  parseGitIgnoreFile,
}
